yarnpkg.com
npm install -g yarn
yarn --version

emit plugin for html

// To install live-server

yarn global add live-server
live-server --version
yarn install --checkfiles

sudo npm install -g babel-cli@6.24.1

//installing presets
yarn init // in your project folder

yarn add babel-preset-react@6.24.1
yarn add babel-preset-env@1.5.2

// To recreate the node-modules in yarn 
yarn install --check-files

// running the babel command
babel src/app.js --out-file=public/scripts/app.js --presets=env,react --watch


// Extension Install - babel ES6/ES7

// Creating a JSX
let template2 = (
    <div>
        <h1>Mriganka Paul</h1>
        <p>Age: 27</p>
        <p>Location: Mississauga</p>
    </div>
)
const appRoot = document.getElementById("app");
ReactDOM.render(template2, appRoot);


React DOM elements - documentation
Rect DOM events - documentation

// Debugger
Chrome React Dev Tools - Install and restart chrome

// life cycle methods are not available for stateless functional components

React life cycle - dcoumentation

// Webpack 

yarn global remove babel-cli live-server

yarn add live-server babel-cli@6.24.1

